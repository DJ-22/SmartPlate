# Copy to .env and fill in real values. Do NOT commit .env.
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=
DB_NAME=SmartPlate

# Required: must be a long random string. Startup will fail if unset or left as placeholder.
JWT_SECRET=
